@model List<Tick_win.Models.UserViewModel>
@for (int i = 0; i < Model.Count(); i++)
{
    <br>
    @Html.ActionLink($"{Model[i].Name}", "UserInfo", "Cashier", new { name = $"{Model[i].Name}" }, new { id = "name" })
    <br>
}
